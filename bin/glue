#!/usr/bin/env coffee
glue = require 'glue'

# 0. Parse command arguments
console.log "glue v#{glue.VERSION}"


# 1. create app
# -> copy all stuff from skel to a new dir
# this includes a Cakefile with tasks to:
# a. pack/build CSS
# b. pack/build js

# 2. create stuff for your app
# -> pluggable?
# ie. RedisModel, CouchModel, GiantCock, whatever
